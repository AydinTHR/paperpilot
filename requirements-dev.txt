# Development / test dependencies.
-r requirements.txt

pytest>=8.2,<9
pytest-cov>=5,<7

# Lint + format. Pinned to one minor so pre-commit and CI format identically.
ruff>=0.15,<0.16

# Local git hooks (pre-commit running ruff + standard checks).
pre-commit>=3.7,<5
